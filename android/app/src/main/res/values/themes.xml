<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.TrustCam" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/brand</item>
        <item name="colorOnPrimary">#FFFFFF</item>
    </style>

    <!-- Viewfinder: pitch black, edge-to-edge, and no system scrim behind the
         gesture bar (the default contrast scrim reads as an ugly translucent
         band at the bottom of the preview) -->
    <style name="Theme.TrustCam.Capture">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>
    </style>
</resources>
